# v2 增强版输出

好的，作为一名资深的测试架构师，我将遵循您的指示，首先进行风险分析，然后设计出覆盖关键异常场景的5条测试用例。

---

### 思考步骤分析

1.  **列出Android端扫码支付的3个关键异常风险点**：
    *   **风险点1：支付请求过程中的网络中断与重连**：用户支付时，网络从WiFi切换到移动数据，或信号突然丢失后恢复，可能导致支付状态不确定（扣款成功但页面未跳转，或重复扣款）。
    *   **风险点2：支付服务器返回异常状态码**：后端服务因过载、bug或维护，返回非标准的HTTP状态码（如502, 503）或业务层面的异常码（如“订单已关闭”），客户端需要优雅处理，避免崩溃或误导用户。
    *   **风险点3：输入参数的边界与异常**：用户输入金额包含特殊字符、超过单笔限额、或扫描了非标准的、已过期的二维码，客户端应有严格的校验逻辑。

2.  **将风险点转化为具体测试场景**：
    *   场景一（网络）：支付请求发送后，立即关闭WiFi并开启移动数据，验证支付结果。
    *   场景二（服务器）：模拟支付服务器返回HTTP 502错误，验证客户端提示。
    *   场景三（服务器）：模拟支付服务器返回业务异常“订单已失效”，验证客户端处理。
    *   场景四（参数）：输入金额超过单笔限额（如5000元），验证前端拦截。
    *   场景五（参数）：扫描一个包含恶意脚本或格式错误的二维码，验证客户端安全性。

3.  **为每个场景编写完整用例**。

### 测试用例

| 编号 | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| TC-PAY-AND-001 | 支付中网络切换（WiFi -> 4G）导致支付结果不确定 | 1. App版本v6.0.1 2. 已登录且账户余额>500元 3. 初始网络为WiFi，4G可用 | 1. 打开扫码页面，扫描有效商户码 2. 输入金额200元 3. 点击确认支付 4. 在支付请求发送后0.5秒内，立即关闭WiFi并开启4G | 1. 页面不崩溃，5秒内显示“支付处理中，请稍候” 2. 10秒后，页面明确显示“支付成功”或“支付失败，余额未变”，无中间态 3. 账户余额最终与显示结果一致 | P1 |
| TC-PAY-AND-002 | 支付服务器返回HTTP 502错误 | 1. App版本v6.0.1 2. 已登录且账户余额>100元 3. 网络正常，使用Charles代理拦截支付接口 | 1. 打开扫码页面，扫描有效商户码 2. 输入金额50元 3. 点击确认支付 4. 在Charles中将支付请求的响应修改为HTTP 502 | 1. 页面5秒内弹出Toast提示：“服务器繁忙，请稍后重试” 2. 支付按钮在3秒内恢复为可点击状态 3. App日志记录此502错误，无ANR或崩溃 | P1 |
| TC-PAY-AND-003 | 支付服务器返回业务异常“订单已过期” | 1. App版本v6.0.1 2. 已登录且账户余额>100元 3. 网络正常，使用Charles模拟支付接口返回业务错误码 | 1. 打开扫码页面，扫描一个已在其他设备支付过的商户码（或通过Mock数据） 2. 输入金额50元 3. 点击确认支付 4. 在Charles中将支付响应JSON中的`code`字段改为`ORDER_EXPIRED` | 1. 页面弹出Dialog提示：“该订单已过期，请重新扫码” 2. Dialog上仅有一个“知道了”按钮 3. 点击“知道了”，页面返回扫码首页，余额不变 | P2 |
| TC-PAY-AND-004 | 输入金额超过单笔限额 | 1. App版本v6.0.1 2. 已登录且账户余额>100000元 3. 网络正常 | 1. 打开扫码页面，扫描有效商户码 2. 在金额输入框中输入`99999.99` 3. 点击确认支付 | 1. 点击支付按钮后，页面无网络请求发出 2. 输入框下方0.5秒内显示红色提示文字：“单笔支付金额不能超过50,000.00元” 3. 支付按钮不可点击，需重新修改金额 | P2 |
| TC-PAY-AND-005 | 扫描包含XSS脚本的恶意二维码 | 1. App版本v6.0.1 2. 已登录 3. 网络正常，准备一个包含`<script>alert('xss')</script>`内容的二维码图片 | 1. 打开扫码页面 2. 将相机对准恶意二维码 3. 等待扫码页面识别 | 1. 页面识别二维码后，不弹出任何JavaScript弹窗或执行脚本 2. 页面显示“无法识别的二维码”或“无效的商户码” 3. App日志无`WebView`加载恶意代码的记录 | P1 |