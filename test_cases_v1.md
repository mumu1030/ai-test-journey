# v1 原始版输出

好的，作为一名资深的Android端测试工程师，针对【扫描支付】功能，我将重点聚焦于异常场景，为你设计以下5条高价值的测试用例。

---

### 测试用例设计

| 编号 | 用例标题 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| TC-001 | 扫描支付-弱网环境下，支付请求超时 | 1. 手机已连接Wi-Fi，并开启网络限速工具（如Charles/Network Emulator），设置为极低带宽（如10KB/s）或高延迟（如1000ms）。<br>2. 应用已登录，且处于扫描支付页面。 | 1. 使用摄像头成功扫描一个有效的收款码。<br>2. 在支付确认页面输入正确的支付密码或验证指纹。<br>3. 点击“确认支付”按钮，观察页面状态。 | 1. 页面出现“支付请求中...”或加载动画，持续时间超过预设超时阈值（如15秒）。<br>2. 超时后，页面提示“网络请求超时，请稍后重试”，支付失败。<br>3. 应用未发生崩溃或闪退，用户可返回上一页或重新尝试支付。 | P0 |
| TC-002 | 扫描支付-支付过程中服务器返回“订单已过期”错误码 | 1. 手机网络正常（4G/5G/Wi-Fi）。<br>2. 应用已登录。<br>3. 使用Mock工具（如Charles）将后端支付确认接口的响应修改为“订单已过期”错误码（如`{"code": 40003, "msg": "订单已过期"}`）。 | 1. 扫描一个有效的收款码，进入支付确认页。<br>2. 输入支付密码并点击“确认支付”。<br>3. 观察页面返回信息及状态。 | 1. 页面立即弹出Toast或对话框提示“支付失败：订单已过期”。<br>2. 用户账户余额/银行卡未发生扣款。<br>3. 应用未崩溃，用户可点击“确定”关闭提示，回到扫描页面。 | P1 |
| TC-003 | 扫描支付-扫描到无效/损坏的二维码（参数异常） | 1. 手机网络正常。<br>2. 应用已登录，且处于扫描支付页面。<br>3. 准备一张打印的、内容为乱码或包含特殊字符的二维码（非标准URL格式）。 | 1. 将摄像头对准该无效二维码。<br>2. 等待应用自动识别。<br>3. 观察应用的反应。 | 1. 应用未能解析该二维码，无任何支付页面跳转。<br>2. 页面底部或中央提示“无法识别二维码”或“无效的收款码”。<br>3. 摄像头扫描框持续处于可扫描状态，用户可继续扫描其他二维码。 | P1 |
| TC-004 | 扫描支付-支付接口返回参数缺失（如缺少“交易流水号”） | 1. 手机网络正常。<br>2. 应用已登录。<br>3. 使用Mock工具（如Charles）将后端支付确认接口的响应体中原有的“tradeNo”字段删除。 | 1. 扫描有效收款码，进入支付确认页。<br>2. 输入正确密码，点击“确认支付”。<br>3. 观察页面行为。 | 1. 客户端解析响应时，因缺少关键字段，应触发异常处理逻辑。<br>2. 页面提示“支付结果未知，请联系客服确认”，或显示“支付异常”。<br>3. 应用未崩溃，且不显示扣款成功页面。用户可在订单列表中查看该笔订单状态为“处理中”或“异常”。 | P1 |
| TC-005 | 扫描支付-支付时服务器返回签名验证失败 | 1. 手机网络正常。<br>2. 应用已登录。<br>3. 使用Mock工具（如Charles）篡改后端支付确认接口返回的响应体中的任意一个字段值（如金额），导致客户端计算出的签名与服务器返回的签名不一致。 | 1. 扫描有效收款码，进入支付确认页。<br>2. 输入正确密码，点击“确认支付”。<br>3. 观察页面行为及日志。 | 1. 客户端校验签名失败，未展示支付成功页面。<br>2. 页面提示“支付结果异常，请稍后查看订单状态”或“数据校验失败”。<br>3. 应用未崩溃，且日志中记录“签名验证失败”错误信息。用户账户应未被实际扣款（需后端配合验证）。 | P2 |